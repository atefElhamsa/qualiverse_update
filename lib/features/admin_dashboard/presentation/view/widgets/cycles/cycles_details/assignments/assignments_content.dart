import 'package:flutter/material.dart';

class AssignmentsContent extends StatelessWidget {
  const AssignmentsContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Assignments'));
  }
}

import 'package:flutter/material.dart';

class CriterionsContent extends StatelessWidget {
  const CriterionsContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Criterions'));
  }
}
